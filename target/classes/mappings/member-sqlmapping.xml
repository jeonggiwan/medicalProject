<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="MemberDAO">
	<select id="login" parameterType="member" resultType="member">
		SELECT ID, PASSWORD
		FROM Member
		WHERE ID = #{id}
	</select>

	<select id="getMemberById" resultType="member">
		SELECT * FROM MEMBER WHERE ID = #{id}
	</select>

	<update id="saveRefreshToken" parameterType="member">
		UPDATE MEMBER SET REFRESH_TOKEN = #{refreshToken}, REFRESH_TOKEN_EXPIRY_DATE
		= #{refreshTokenExpiryDate} WHERE ID = #{id}
	</update>

	<update id="updateRefreshToken" parameterType="member">
		UPDATE MEMBER SET REFRESH_TOKEN = #{refreshToken}, REFRESH_TOKEN_EXPIRY_DATE
		= #{refreshTokenExpiryDate} WHERE ID = #{id}
	</update>

	<update id="removeRefreshToken" parameterType="string">
		UPDATE MEMBER SET REFRESH_TOKEN = NULL, REFRESH_TOKEN_EXPIRY_DATE = NULL
		WHERE ID = #{id}
	</update>

	<select id="getRefreshTokenByUserId" parameterType="string"
		resultType="member">
		SELECT ID, REFRESH_TOKEN as refreshToken, REFRESH_TOKEN_EXPIRY_DATE as
		refreshTokenExpiryDate
		FROM MEMBER
		WHERE ID = #{id}
	</select>
	<delete id="removeExpiredRefreshTokens">
		UPDATE MEMBER SET REFRESH_TOKEN = NULL, REFRESH_TOKEN_EXPIRY_DATE = NULL
		WHERE REFRESH_TOKEN_EXPIRY_DATE IS NOT NULL AND
		REFRESH_TOKEN_EXPIRY_DATE &lt; CURRENT_TIMESTAMP
	</delete>
	
	    <select id="getAllMembers" resultType="member">
        SELECT * FROM MEMBER
    </select>

    <select id="searchMembers" parameterType="map" resultType="member">
        SELECT * FROM MEMBER
        WHERE 1=1
        <if test="id != null and id != ''">
            AND ID LIKE CONCAT('%', #{id}, '%')
        </if>
        <if test="name != null and name != ''">
            AND NAME LIKE CONCAT('%', #{name}, '%')
        </if>
    </select>
</mapper>