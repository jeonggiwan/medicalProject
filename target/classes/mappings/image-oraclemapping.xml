<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ImageDAO">

<select id="getImagePath" resultType="string" parameterType="map">
    SELECT CONCAT(path, fName) FROM IMAGETAB WHERE STUDYKEY=#{studyKey} AND SERIESKEY=#{seriesKey};
</select>
